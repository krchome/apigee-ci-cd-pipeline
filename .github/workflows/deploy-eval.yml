name: Deploy API Proxy to EVAL

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-to-eval:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Update and install beta components
        run: |
          gcloud components update --quiet
          gcloud components install beta --quiet
          gcloud --version

      - name: Import and Deploy API Proxy Revision
        id: deploy_proxy
        run: |
          echo "Zipping Apigee proxy source..."
          zip -r hello-world.zip apiproxy

          echo "Deploying proxy bundle to Apigee X using archives..."
          REVISION=$(gcloud beta apigee archives deploy \
            --project="${{ secrets.GCP_PROJECT_ID }}" \
            --environment="eval" \
            --source="hello-world.zip" \
            --format="value(revision)")

          echo "Deployed revision: $REVISION"
          echo "imported_revision=$REVISION" >> $GITHUB_OUTPUT

      - name: Complete job
        run: echo "✅ Successfully deployed hello-world proxy to Apigee X eval environment!"
